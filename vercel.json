{
  "functions": {
    "src/app/api/**/*.ts": {
      "maxDuration": 30
    }
  },
  "crons": [],
  "framework": "nextjs",
  "buildCommand": "rm -rf .next node_modules/.cache && prisma generate && npm run build"
}